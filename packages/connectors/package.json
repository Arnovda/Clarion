{
  "name": "@databridge/connectors",
  "version": "0.1.0",
  "description": "Source connector framework + implementations. Shared between backend and sync-worker.",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "lint": "eslint src --ext .ts",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@azure/storage-blob": "^12.31.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "axios": "^1.19.0",
    "duckdb-async": "^1.4.2",
    "fast-xml-parser": "^4.5.0"
  },
  "devDependencies": {
    "@types/json-schema": "^7.0.15",
    "@types/node": "^20.16.5",
    "nock": "^13.5.5",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  },
  "//overrides": [
    "Transitive advisories we cannot reach by bumping a direct dependency.",
    "Each entry is the lowest version that clears the advisory (keep the pin",
    "in step with what backend/package.json carries where both hold the same",
    "package).",
    "  fast-uri  3.0.0-3.1.5  HIGH  four GHSAs (ajv -> fast-uri); ajv validates",
    "                               connector config at runtime, so this is",
    "                               runtime-exposed and must be fixed, never",
    "                               allowlisted in scripts/audit-gate.mjs."
  ],
  "overrides": {
    "fast-xml-builder": "^1.2.0",
    "fast-uri": "^3.1.6",
    "nanoid": "^3.3.17"
  }
}

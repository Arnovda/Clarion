per-tenant

# Re-applied 2026-08-03: promotes image main-a7ad497 (foreign keys judged by
# containment against a measured key instead of value overlap). Applied AFTER
# deploy.yml run 386 reported success, per the lesson below.
#
# Re-applied 2026-07-30 (second): the previous application raced its own build.
# The corrected merge key and the promote landed in ONE commit, so this workflow
# fired while deploy.yml was still building the image — creating a revision from
# the app's THEN-current template, i.e. the OLD build. This re-application runs
# after the build and picks up the image that actually contains the fix.
#
# LESSON: merge the code first, wait for deploy.yml to finish, THEN touch this
# file. This control promotes whatever the app template points at RIGHT NOW; it
# does not wait for a build.
#
# The mode itself is unchanged. This workflow waits for the NEW revision to reach
# Provisioned and fails loudly otherwise, which deploy.yml never does and
# promote.yml cannot (it shifts to latestReadyRevisionName, silently promoting
# the previous revision if the new one failed to boot).

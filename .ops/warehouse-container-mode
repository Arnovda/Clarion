per-tenant

# Re-applied 2026-07-28 (second): rolls backend image main-5d2b327 to 100%
# traffic. That image carries the per-connection cache invalidation, the
# transaction-local tenant context in the BullMQ workers (the last fail-open
# cross-tenant hazard), and the fail-closed fix to the relationship ownership
# fallback that CI caught turning refusals into 500s.
#
# Promote vehicle on purpose: this workflow waits for the NEW revision to reach
# Provisioned and fails loudly otherwise. deploy.yml never waits, and
# promote.yml shifts to latestReadyRevisionName -- which silently promotes the
# PREVIOUS revision if the new one failed to boot.

backend + frontend

# Re-applied 2026-08-20 (seventh): promotes image main-f60b91a — the Build
# page's "Ask about your subjects" chat (coverage answers from the real
# catalog) and the additive extend flow (one new subject next to the
# existing build, collisions refused in code). Applied AFTER Build & Deploy
# run 476 reported success.
#
# Re-applied 2026-08-20 (sixth): promotes image main-71f29a8 — the Build
# page's working pane drops the streamer's diag plumbing (only real AI
# reasoning renders) and the headline shows live design progress
# ("N tables drafted so far"). Applied AFTER Build & Deploy run 473
# reported success.
#
# Re-applied 2026-08-20 (fifth): promotes image main-e5242da — the Subjects
# hub under Uncover replaces the per-topic rail rows (Option A of the mocked
# navigation directions). Applied AFTER Build & Deploy for e5242da reported
# success.
#
# Re-applied 2026-08-20 (fourth): promotes image main-7bade8a — column
# lineage derived deterministically from the transformation SQL at build
# time, and the lineage graph's Databricks-style row cap. Applied AFTER
# Build & Deploy run 469 reported success.
#
# Re-applied 2026-08-20 (third): promotes image main-b597605 — the schema
# diagram's join columns ship past the is_technical firewall (edges land on
# named fields again) and the builder synthesizes the dim_date relationship
# rows the AI omits. Applied AFTER Build & Deploy run 467 reported success.
#
# Re-applied 2026-08-20 (second): promoted frontend main-3d69c96 — Manage
# mode's two diagrams rebuilt (schema in the relations language, lineage on
# the catalog's LineageGraph). Frontend only; the backend was unchanged.
#
# Created 2026-08-19: first application. Promoted commit 5c40b13 — the Build
# page run panel (topic cards + AI-reasoning disclosure), the zero-row Delta
# build fix, the "waiting for data" topic state, and row-count-grounded AI
# design. Applied AFTER the Build & Deploy run for 5c40b13 reported success,
# per the LESSON in promote.yml.

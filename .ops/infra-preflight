run 8 — does the jobs-worker ALSO register schedules? Read ROLE and RUN_SCHEDULERS on both apps.

Run 7 refuted the queue-ownership theory: the jobs-worker's WORKER_QUEUES does
include `warehouse-maintenance`, the app is Running on the current image, and
startMaintenanceWorker() is called at workers.ts:593. So the weekly sweep is
registered and consumed and still never fires.

Run 7 did not print the one thing that would explain it. `index.ts` gates
schedule registration on RUN_SCHEDULERS: any process where it is not the
string "false" calls loadSchedules(), and loadSchedules() calls
registerWeeklyMaintenance(), which REMOVES the existing repeatable and re-adds
it. If BOTH containers do that, they delete each other's pending run every
time either one boots — and the backend runs at min_replicas 0, so it boots
often.

What settles it, and run 7 reported none of them:
  - worker ROLE
  - worker RUN_SCHEDULERS   <- if this is not exactly "false", both register
  - backend RUN_SCHEDULERS

Read-only, as always.
